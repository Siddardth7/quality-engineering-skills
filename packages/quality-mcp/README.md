# quality-mcp

Model Context Protocol (MCP) server workspace member for the Quality Platform.

## Overview

`quality-mcp` serves as the MCP server package that binds `quality-core` deterministic engines to Model Context Protocol endpoints, enabling AI agents to interact with quality engineering calculations, schemas, and analytical tools.
