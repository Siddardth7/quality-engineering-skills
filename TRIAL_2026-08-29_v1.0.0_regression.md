# TRIAL_2026-08-29_v1.0.0_regression

**Milestone:** v1.0.0 — End-to-End Skill Regression (#150 / E10)
**Run date:** 2026-08-29
**Environment:** Python 3.11.15, uv 0.11.21, macOS 26.5.2 (arm64), quality-mcp 0.9.0, quality-core 0.9.0
**Command(s) run:**
`uv run pytest packages/quality-mcp/tests/test_e2e_catalog_regression.py -q` → **11 passed**
`uv run pytest packages/quality-mcp --cov=quality_mcp --cov-fail-under=100 -q` → **619 passed, coverage 100.00%**
`uv run ruff check .` → clean · `uv run mypy` → clean (85 source files)

Suite: `packages/quality-mcp/tests/test_e2e_catalog_regression.py`.

---

## Catalog exercised (one FastMCP client session)

All 31 registered tools called inside a **single** `create_connected_server_and_client_session`,
each over its own shipped benchmark/default input. The walk also asserts that the set of tools it
exercised equals the set the server advertises, so an unexercised tool fails the test.

| # | Domain | Compute tool(s) | Canvas tool(s) | Result |
|---|---|---|---|---|
| 1 | FMEA | `lookup_fmea_ap` | `render_fmea_canvas` | PASS |
| 2 | SPC | `calculate_spc_chart` | `render_spc_canvas` | PASS |
| 3 | MSA | `calculate_gage_rr` | `render_msa_canvas` | PASS |
| 4 | Control Plan | `validate_control_plan` | `render_controlplan_canvas` | PASS |
| 5 | RCA | `validate_5why`, `categorize_fishbone`, `scope_is_is_not` | `render_5why_canvas`, `render_fishbone_canvas`, `render_isisnot_canvas` | PASS |
| 6 | NCR + COPQ (one combined domain per E7/#147) | `write_ncr`, `recommend_disposition`, `estimate_copq` | `render_ncr_canvas`, `render_copq_canvas` | PASS |
| 7 | PPAP | `audit_ppap_package`, `lookup_ppap_requirement`, `validate_psw`, `assess_ppap_capability` | `render_ppap_canvas` | PASS |
| 8 | SQE | `calculate_supplier_ppm`, `calculate_otif`, `calculate_vendor_scorecard`, `evaluate_escalation`, `generate_scar` | `render_sqe_canvas` | PASS |
| — | server health | `ping` | — | PASS |

Note on `recommend_disposition`: `disposition` is legitimately `null` on the benchmark input (the
engine withholds a disposition until the evidence supports one), so the shape assertion for that
tool is on `verdict`, not `disposition`.

---

## Exporter + E1 live-formula verifier results

No exporter is registered as an MCP tool — `quality_mcp` carries zero export bindings — so each
`quality_core` exporter is called **directly** from the suite on the same domain benchmark the tool
leg used. Verifier: `assert_cell_is_formula` (duplicated into
`packages/quality-mcp/tests/_xlsx_formula_audit.py`; see note below). Coords are the ones each
domain's own `test_*_export.py` already proved, not new guesses.

| Domain | Exporter | Verifier result | Cells checked |
|---|---|---|---|
| FMEA | `export_fmea_workbook` | PASS (live) | `FMEA!<RPN col>2` — `=S*O*D` |
| SPC | `export_spc_excel` | PASS (live) | `Control Chart Data!G2`; `Process Capability!B7` (Cp), `!B8` (Cpk) |
| MSA | `export_msa_workbook` | PASS (live) | `Gage R&R Summary!B10` (ndc), `!D13` (EV %SV) |
| Control Plan | `export_controlplan_workbook` | PASS (live, roll-ups only) | `Coverage!B2`, `!B3`, `!B4` |
| RCA | `export_rca_workbook` | PASS (structured-only, **N/A confirmed**) | `assert_cell_is_formula` **raises** on `5-Why Analysis!B2`, `6M Fishbone!B2`, `Kepner-Tregoe Is-Is Not!B2` |
| NCR | `export_ncr_workbook` | PASS (live, roll-ups only) | `Dispositions & Containment!B2` (COUNTIF), `!C2` (SUMIF); `Summary & Metadata!B4`, `!B5` |
| COPQ | `export_copq_workbook` | PASS (live) | `COPQ Ledger!F2`; `PAF Category Summary!C2`, `!D2`; `Executive Summary & Metadata!B4` |
| PPAP | `export_ppap_workbook` | PASS (live) | `Completeness!B6`, `!B7` (COUNTA / COUNTIF) |
| SQE | `export_sqe_workbook` | PASS (live, **vendor-rating only**) | `SQE Vendor Scorecard!<PPM>2`, `!<OTIF>2`, `!<Composite_Score>2` |

**RCA is the qualitative-domain path.** `quality_core/rca/export.py` declares arithmetic
live-formula verification N/A for RCA by design (RULE 6 in `rca/ASSUMPTIONS_LOG.md`). Asserting a
formula there would assert a defect, so the suite asserts the verifier **raises** on RCA cells —
the same proof `test_rca_export.py` uses. The declaration is therefore enforced, not merely
claimed, and it is not a skip.

**SQE SCAR/8D export is deferred and out of scope per #199 (Option A).** Only the vendor-rating
half of the SQE domain has an exporter; the suite does not attempt a SCAR workbook.

---

## Negative control

One malformed dataset, one live tool, in its own session — with a valid call before and after so
the malformed call is proven to have surfaced an error *and* not corrupted the session.

| Call | Payload | Result |
|---|---|---|
| `calculate_spc_chart` (baseline) | `{"chart_type": "Xbar-R", "data": <benchmark subgroups>}` | `isError=False` (PASS) |
| `calculate_spc_chart` (malformed) | `{"chart_type": "Xbar-R", "data": "not-a-list"}` | `isError=True` (PASS — no silent pass) |
| `calculate_spc_chart` (same-session follow-up) | baseline payload | `isError=False`, byte-identical to baseline (PASS — session not corrupted) |

---

## Skill catalog note

11 skill directories under `skills/` (excluding `_template/` and `mcp-health/`, which are not
domain skills) are covered via their underlying MCP tools: `5why-root-cause`, `control-plan`,
`copq-estimator`, `fishbone-analysis`, `fmea-reviewer`, `is-is-not-scoping`, `msa-gauge-rr`,
`ncr-writing`, `ppap-checker`, `spc-control-charts`, `supplier-scar`.

`docs/milestones/v1.0.0.md` and issue #150 state "10 skills" but then list 11 — a **pre-existing
doc discrepancy of one**, not resolved here. Milestone-doc governance belongs to #151 (E11
closeout).

---

## Supporting notes

- `packages/quality-mcp/tests/_xlsx_formula_audit.py` is a verbatim duplicate (plus a
  why-it-exists note) of the quality-core copy. The root `pyproject.toml` sets
  `--import-mode=importlib`, so a test file's own directory is not on `sys.path`;
  `packages/quality-mcp/tests/conftest.py` adds that one line, exactly as
  `packages/quality-core/tests/conftest.py` already does for the same helper problem. The
  conftest holds no fixtures — the package's duplicate-fixtures-per-module convention is
  unchanged.

## Summary

**11/11 tests passed** in the E10 suite; 8/8 domains PASS at the tool layer; 9/9 exporters PASS at
the verifier layer (8 live-formula, 1 structured-only/N/A confirmed); negative control PASS.
`uv run pytest` green, `ruff check .` clean, `mypy` clean, quality-mcp coverage **100.00%**
(line + branch, unchanged).
